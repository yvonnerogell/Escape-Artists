<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Game.Views.Characters.CharacterIndexPage"
             Title="Characters">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Add" Clicked="AddItem_Clicked" IconImageSource="icon_add.png"/>
        <ToolbarItem Text="Read" Clicked="ReadItem_Clicked" IconImageSource="icon_new.png"/>
    </ContentPage.ToolbarItems>

    <ContentPage.Content>

        <FlexLayout
            BindableLayout.ItemsSource="{Binding Dataset}"
            Wrap="Wrap"
            Direction="Row"
            JustifyContent="Center"
            AlignItems="Center"
            AlignContent="Start">
            <BindableLayout.ItemTemplate>

                <DataTemplate>
                    <StackLayout>
                        <ImageButton Source="item.png"/>
                        <Label Text="{Binding Name}"/>
                            <Label Text="{Binding Level}"/>
                    </StackLayout>
                </DataTemplate>
                
            </BindableLayout.ItemTemplate>
            
            
        </FlexLayout>


    </ContentPage.Content>


</ContentPage>